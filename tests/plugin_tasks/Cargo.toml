[package]
name = "plugin_tasks"
version = "0.1.0"
edition = "2024"

[dependencies]
plugin-manager = { path = "../../{{ crate_name }}_plugin_manager" }

[lib]
name = "plugin_tasks"
crate-type = ["lib", "cdylib"]