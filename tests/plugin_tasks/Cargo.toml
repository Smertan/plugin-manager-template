[package]
name = "plugin_tasks"
version = "0.1.0"
edition = "2024"

[dependencies]
plugin-manager = { path = "../../{{ project-name }}_plugin_manager" }
# plugin-types = { path = "../../plugin_types" }

[lib]
name = "plugin_tasks"
crate-type = ["lib", "cdylib"]